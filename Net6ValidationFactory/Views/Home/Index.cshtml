
@model Net6ValidationFactory.Models.Entities.User


<h1>Index</h1>

<p>
    <a>Fluent Validation Example</a>
</p>

@using (Html.BeginForm("Index", "Home", FormMethod.Post))
{
    @Html.AntiForgeryToken()

    <div class="form-group">
        @Html.LabelFor(x => x.Mail)
        @Html.TextBoxFor(x => x.Mail, new {@class = "form-control", placeholder = "Mail"})
        @Html.ValidationMessageFor(x => x.Mail, null, new {@class = "invalid-feedback d-block"})
    </div>
    <div class="form-group">
        @Html.LabelFor(x => x.Ad)
        @Html.TextBoxFor(x => x.Ad, new {@class = "form-control", placeholder = "Ad"})
        @Html.ValidationMessageFor(x => x.Ad,null, new {@class = "invalid-feedback d-block"})
    </div>
        <div class="form-group">
        @Html.LabelFor(x => x.Sifre)
        @Html.TextBoxFor(x => x.Sifre, new {@class = "form-control", placeholder = "Şifre"})
        @Html.ValidationMessageFor(x => x.Sifre, null, new {@class = "invalid-feedback d-block"})
    </div>
    <button class="btn btn-xs btn-primary" type="submit">Submit</button>
}